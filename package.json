{
  "name": "sooda-saas-platform",
  "private": true,
  "version": "0.1.0-alpha",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --external:./src/tests/* --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "test:phase0": "tsx -e \"import { Phase0TestSuite } from './src/tests/phase0.test.ts'; const r = Phase0TestSuite.runAll(); console.table(r); if(r.some(x=>!x.passed)){ process.exit(1); }\"",
    "test:security": "tsx src/tests/security.test.ts",
    "test:remediation": "tsx src/tests/phase0_remediation.test.ts",
    "test:audit": "tsx src/tests/audit_tamper.test.ts",
    "test:audit:startup": "tsx src/tests/audit_startup_integrity.test.ts",
    "test:failure": "tsx src/tests/audit_failure.test.ts",
    "test:auth": "tsx src/tests/authentication.test.ts",
    "test:stores": "tsx src/tests/store_management.test.ts",
    "test": "npm run test:phase0 && npm run test:security && npm run test:remediation && npm run test:audit && npm run test:audit:startup && npm run test:failure && npm run test:auth && npm run test:stores",
    "clean": "rm -rf dist server.js data/*.db *.db *.sqlite *.sqlite3 sooda.db release.zip",
    "package": "python3 scripts/package-release.py",
    "verify:clean": "python3 -c \"import glob, os, sys; dbs = [f for p in ['**/*.db', '**/*.sqlite', '**/*.sqlite3'] for f in glob.glob(p, recursive=True) if not f.startswith('node_modules')]; (print('VERIFIED: Zero runtime database files found.') or sys.exit(0)) if not dbs else (print('FAILED: Found DB files:', dbs) or sys.exit(1))\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@node-rs/argon2": "^2.2.1",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
